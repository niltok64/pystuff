use unix_build_all.sh for unix-based systems like macOS and linux

use windows_build_all.bat for windows lel

if you just want to build the programs seperately install pyinstaller using "pip install pyinstaller" then run "pyinstaller [name of program].py --onefile"
